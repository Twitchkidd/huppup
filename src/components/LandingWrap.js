import styled from 'styled-components';

const AppWrap = styled.div`
	width: min(100%, 64rem);
	margin-inline: auto;
	background: linear-gradient(var(--pink-900), var(--blue-900));
	min-height: 100vh;
`;

export default AppWrap;
